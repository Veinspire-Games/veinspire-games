---
/**
 * HERO секция "как у рефа":
 * - полноэкранный фон (cover) + мягкий затемняющий градиент
 * - центрированный блок: H1 (3 строки), абзац, кнопка "Learn More" с круглой иконкой
 * - плавный скролл к следующему блоку (href="#about")
 *
 * Как сменить фон:
 *   в hero.module.scss у .hero задаётся переменная --hero-bg: url('/images/hero.jpg')
 *   либо пробросить прямо на секции: <section class={s.hero} style="--hero-bg:url('/path.jpg')">
 */
import s from "../styles/hero.module.scss";
---

<section id="hero" class={s.hero} aria-label="Hero">
  <div class={s.overlay} aria-hidden="true"></div>

  <div class={`container ${s.center}`}>
    <h1 class={s.title}>
      Game-dev experts.<br/>
      Fire in our eyes.<br/>
      Cool heads.
    </h1>

    <p class={s.lead}>
      We design stories players can play, <br/>
      and games they’ll never forget.<br/>
      Let’s create what others dream about!
    </p>

    <a href="#about" class={s.learn} aria-label="Learn more">
      <span class={s.pill} aria-hidden="true">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none">
          <path d="M6 9l6 6 6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </span>
      <span class={s.learnText}>Learn More</span>
    </a>
  </div>
</section>
