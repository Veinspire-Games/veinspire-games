---
import s from "../styles/talk.module.scss";
---

<section class={s.wrap} aria-label="Let’s talk">
  <div class={s.inner}>
    <a href="/contact" class={s.link}>
      <span class={s.text}>
        <span class={s.word}>LET’S</span>
        <span class={s.word}>TALK</span>
      </span>
      <span aria-hidden="true" class={s.arrow}>→</span>
    </a>

    <p class={s.note}>GET IN TOUCH TODAY!</p>
  </div>
</section>
